<UserControl x:Class="SparePartsManager.Views.BasicDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:SparePartsManager.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:BasicDataViewModel, IsDesignTimeCreatable=True}"
             Background="#F0F4F8">

    <UserControl.Resources>
        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="14,0">
                            <TextBlock Text="{TemplateBinding Content}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="基础信息维护" FontSize="20" FontWeight="Bold"
                   Foreground="#1E3A5F" Margin="0,8,0,6"/>
        <TextBlock Grid.Row="1" Text="维护规格、型号、厂家、项目基础字典；备件界面只能从这些字典中选择，无法直接新增。"
                   FontSize="12" Foreground="#888" Margin="0,0,0,16"/>

        <!-- 工具栏 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16,12"
                Margin="0,0,0,0" BorderBrush="#E0E6ED" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="新增" Width="100"
                            Background="#007ACC" Foreground="White" Style="{StaticResource ToolButtonStyle}"
                            Command="{Binding AddCommand}"/>
                    <Button Content="编辑" Width="100" Margin="8,0,0,0"
                            Background="#FFB90F" Foreground="White" Style="{StaticResource ToolButtonStyle}"
                            Command="{Binding EditCommand}"/>
                    <Button Content="删除" Width="100" Margin="8,0,0,0"
                            Background="#DC3545" Foreground="White" Style="{StaticResource ToolButtonStyle}"
                            Command="{Binding DeleteCommand}"/>
                    <TextBlock Text="提示：已被备件使用的条目不允许删除" FontSize="11"
                               Foreground="#999" VerticalAlignment="Center" Margin="16,0,0,0"/>
                </StackPanel>

                <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}">
                    <TabItem Header="规格">
                        <DataGrid ItemsSource="{Binding Specifications}" SelectedItem="{Binding SelectedSpecification}"
                                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  RowHeaderWidth="0" Background="White" GridLinesVisibility="Horizontal"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  FontSize="13" Foreground="#444" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="被备件使用数" Binding="{Binding RefCount}" Width="120"/>
                            </DataGrid.Columns>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F7FA"/>
                                    <Setter Property="Foreground" Value="#1E3A5F"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Height" Value="38"/>
                                    <Setter Property="BorderBrush" Value="#E0E6ED"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Height" Value="36"/>
                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="型号">
                        <DataGrid ItemsSource="{Binding Models}" SelectedItem="{Binding SelectedModel}"
                                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  RowHeaderWidth="0" Background="White" GridLinesVisibility="Horizontal"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  FontSize="13" Foreground="#444" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="被备件使用数" Binding="{Binding RefCount}" Width="120"/>
                            </DataGrid.Columns>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F7FA"/>
                                    <Setter Property="Foreground" Value="#1E3A5F"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Height" Value="38"/>
                                    <Setter Property="BorderBrush" Value="#E0E6ED"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Height" Value="36"/>
                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="厂家">
                        <DataGrid ItemsSource="{Binding Manufacturers}" SelectedItem="{Binding SelectedManufacturer}"
                                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  RowHeaderWidth="0" Background="White" GridLinesVisibility="Horizontal"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  FontSize="13" Foreground="#444" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="被备件使用数" Binding="{Binding RefCount}" Width="120"/>
                            </DataGrid.Columns>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F7FA"/>
                                    <Setter Property="Foreground" Value="#1E3A5F"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Height" Value="38"/>
                                    <Setter Property="BorderBrush" Value="#E0E6ED"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Height" Value="36"/>
                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="项目">
                        <DataGrid ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}"
                                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                                  RowHeaderWidth="0" Background="White" GridLinesVisibility="Horizontal"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  FontSize="13" Foreground="#444" BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="被备件使用数" Binding="{Binding RefCount}" Width="120"/>
                            </DataGrid.Columns>
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F5F7FA"/>
                                    <Setter Property="Foreground" Value="#1E3A5F"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Height" Value="38"/>
                                    <Setter Property="BorderBrush" Value="#E0E6ED"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Height" Value="36"/>
                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
