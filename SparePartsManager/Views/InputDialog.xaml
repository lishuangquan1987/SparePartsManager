<Window x:Class="SparePartsManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新增"
        Width="380" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F0F4F8"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="InputDialogButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="16,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="请输入：" FontSize="14"
                   Foreground="#1E3A5F" Margin="0,0,0,12"/>

        <TextBox x:Name="InputBox" Grid.Row="1"
                 Height="32" VerticalAlignment="Top"
                 BorderBrush="#CCC" BorderThickness="1"
                 Padding="8,4" FontSize="14"
                 TextChanged="InputBox_TextChanged"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="取消" Width="90"
                    Style="{StaticResource InputDialogButtonStyle}"
                    Background="#E8E8E8" Foreground="#555"
                    Click="CancelButton_Click"/>
            <Button x:Name="OkButton" Content="确定" Width="90"
                    Style="{StaticResource InputDialogButtonStyle}"
                    Background="#007ACC" Foreground="White"
                    Margin="12,0,0,0" IsEnabled="False"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
